{
  "name": "hippolambdaproxy",
  "version": "1.0.0",
  "description": "AWS lambda function that passes Alexa skill messages to backend lambda",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maarten ter Mors",
  "license": "ISC",
  "dependencies": {
    "promise": "^8.0.2"
  }
}
